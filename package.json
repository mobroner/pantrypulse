{
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.0",
    "express": "^4.18.2",
    "itty-router": "^4.0.22",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.11.3",
    "bcrypt": "^5.1.0"
  }
}
